# macOS
.DS_Store

# Editor
.vscode/
.idea/

# Local env / secrets (never commit Supabase service keys or Anthropic keys)
.env
.env.local
config.local.js

# Logs
*.log

# Dependencies (none expected — static site, no build step)
node_modules/
